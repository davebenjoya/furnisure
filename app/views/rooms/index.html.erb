<div class="container">
  <% @rooms.each do |room| %>
    <%= link_to room_path(room) do %>
    <div class="card w-25 p-1 mt-1">
      <p class="card-title mt-3"><%= room.name %></p>
    </div>
    <% end %>
  <% end %>
</div>

